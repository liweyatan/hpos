# 错题集 — Java后端面试准备

> 基于 HPOS（智慧医院挂号系统）项目面试复盘
> 日期：2026-06-03

---

## 一、Java 原理篇

### 1. Spring Boot vs Spring 框架区别

| | Spring | Spring Boot |
|--|--------|-------------|
| 配置 | 手动写大量 XML | 自动配置（Auto Configuration） |
| 依赖 | 手动管理版本冲突 | Starter 一键引入，版本由 parent 统一管理 |
| 部署 | 要装 Tomcat，打 WAR 包 | 内嵌 Tomcat，`java -jar` 直接运行 JAR |
| 监控 | 无 | Actuator 提供生产级监控 |

**核心优势：** 自动配置 + 起步依赖 + 内嵌服务器 + 生产监控

---

### 2. MyBatis-Plus vs MyBatis

| 功能 | MyBatis | MyBatis-Plus |
|------|---------|-------------|
| 基础 CRUD | 手写 SQL + XML | 继承 `BaseMapper<T>` 自动拥有 |
| 条件查询 | 手写 `<where>` + `<if>` | `LambdaQueryWrapper` 链式调用 |
| 分页 | 手写 `LIMIT` + `COUNT` | `Page` 分页插件一键搞定 |
| 自动填充 | 无 | `MetaObjectHandler` 自动填充 createTime 等 |
| 逻辑删除 | 手写 | `@TableLogic` 注解 |

**要点：** MyBatis-Plus 只是增强，不是替代。复杂 SQL 仍然要手写。

---

### 3. `@Transactional` 事务传播机制

7 种传播行为，重点掌握前 3 个：

| 传播行为 | 说明 | 常用场景 |
|---------|------|---------|
| `REQUIRED`（默认） | 有事务就用，没有就新建 | 通用业务方法 |
| `REQUIRES_NEW` | 不管有没有都新开，挂起当前 | 日志记录（不随主事务回滚） |
| `SUPPORTS` | 有就用，没有就不用 | 查询方法 |
| `NEVER` | 不能有事务，有就抛异常 | 测试 |

**项目示例：** `createOrder()` 用 `@Transactional` 保证扣号源 + 创建订单同时成功或同时失败。

---

### 4. HTTP GET vs POST

| | GET | POST |
|--|-----|------|
| 用途 | 查询 | 创建/提交 |
| 参数位置 | URL 上（`?key=value`） | 请求体（Body） |
| 参数长度 | 有限制（浏览器限制） | 无限制 |
| 安全性 | 参数暴露在 URL | Body 相对安全 |
| 缓存 | 会被浏览器缓存 | 不会缓存 |
| 幂等 | ✅ 是 | ❌ 否 |

---

### 5. 密码存储：MD5 vs BCrypt

| 算法 | 安全性 | 说明 |
|------|--------|------|
| MD5 | ❌ 不安全 | 已经被彩虹表破解，容易被撞库 |
| BCrypt | ✅ 安全 | 自带盐值（Salt），每次加密结果不同，计算速度慢 → 抵抗暴力破解 |

**项目现状：** 用了 `MD5Utils.java`（Hutool 实现），需要改为 `BCryptPasswordEncoder`。

---

### 6. Lombok `@Data` 注解

**`@Data` 组合了以下注解：**
- `@Getter` — 所有字段生成 getter
- `@Setter` — 所有字段生成 setter
- `@ToString` — 生成 toString()
- `@EqualsAndHashCode` — 生成 equals() 和 hashCode()
- `@RequiredArgsConstructor` — 生成带必要参数（final 字段）的构造器

**⚠️ 坑：** `@EqualsAndHashCode` 默认用所有字段比，在 MyBatis-Plus 实体中应指定 `@EqualsAndHashCode(callSuper=false, of={"id"})`，否则逻辑删除字段 `deleted` 会导致两个相同 ID 的对象不等。

---

### 7. 三层架构职责

```
Controller → Service → Mapper → Database
    ↑            ↑         ↑
  接收请求     业务逻辑   数据库操作
  参数校验     编排流程   SQL/ORM
  返回响应     事务管理
```

| 层 | 职责 | 禁止做的事 |
|----|------|-----------|
| Controller | 接请求、调 Service、返回 JSON | ❌ 不能写 if/for 业务逻辑 |
| Service | 写业务规则、事务控制 | ❌ 不能直接操作 HttpServletRequest |
| Mapper | 数据库 CRUD | ❌ 不能写业务逻辑 |
| Entity | 表映射，纯数据容器 | ❌ 不能写业务方法 |

**分层目的：** 解耦、可测试、可维护（换数据库只改 Mapper）。

---

## 二、项目相关问题篇

### 8. 号源超卖问题（并发安全）

**问题场景：** 两个用户同时抢最后一个号，都查到 `available_count = 1`，都扣减，结果超卖。

**当前代码问题（`RegistrationOrderServiceImpl`）：**
```java
// ❌ 先查询再更新 — 非线程安全
RegistrationSource source = sourceMapper.selectById(id);
if (source.getAvailableCount() <= 0) { throw ...; }
source.setAvailableCount(source.getAvailableCount() - 1);
sourceMapper.updateById(source);
```

**正确方案（原子 SQL）：**
```sql
UPDATE registration_source
SET available_count = available_count - 1
WHERE id = ? AND available_count > 0;
```

Java 代码判断：
```java
int rows = registrationSourceMapper.deductSource(sourceId);
if (rows == 0) { throw new RuntimeException("号源已满"); }
```

**其他方案：** 悲观锁（`SELECT ... FOR UPDATE`）、Redis 分布式锁、乐观锁（版本号）。

---

### 9. 项目认证与安全问题

**当前实现：**
- 登录方式：用户名 + 密码 → 查库比对 → 返回用户信息
- 没有生成 JWT Token
- `patientId` 作为 URL 参数传递（不安全，可以伪造）

**生产级做法：**
1. 登录时生成 JWT Token（含用户 ID、过期时间）
2. 前端每次请求在 Header 带 `Authorization: Bearer xxx`
3. 后端用拦截器/过滤器解析 Token 获取用户 ID
4. 使用 Spring Security / Shiro 做权限管理

---

### 10. RESTful 规范

**核心原则：** 用 HTTP 方法表达操作，用 URL 表达资源

| 操作 | RESTful 写法 | 你项目的写法 |
|------|-------------|-------------|
| 查科室列表 | `GET /api/departments` | ✅ 正确 |
| 查单个科室 | `GET /api/departments/{id}` | ✅ 正确 |
| 创建订单 | `POST /api/orders` | ✅ 正确 |
| 取消订单 | `PUT /api/orders/{id}/cancel` | ⚠️ 应该用 `DELETE /api/orders/{id}` |
| 更新订单 | `PUT /api/orders/{id}` | 暂缺 |
| 查患者 | `GET /api/patients/phone/{phone}` | ✅ 正确 |

---

### 11. 项目各层文件对应关系（理解全貌）

```
Entity （表映射）
  ├── Department.java          → 部门表
  ├── Doctor.java              → 医生表
  ├── RegistrationSource.java  → 号源表
  ├── Patient.java             → 患者表
  ├── RegistrationOrder.java   → 订单表
  └── SysUser.java             → 用户表

Mapper （继承 BaseMapper，自动 CRUD）
  ├── DepartmentMapper.java
  ├── DoctorMapper.java
  ├── RegistrationSourceMapper.java
  ├── PatientMapper.java
  ├── RegistrationOrderMapper.java
  └── SysUserMapper.java

Service （业务接口 + 实现）
  └── impl/  → ServiceImpl<Mapper, Entity> 批量模板方法

Controller （REST API）
  ├── AuthController.java              → 登录
  ├── DepartmentController.java        → 科室
  ├── DoctorController.java            → 医生
  ├── RegistrationSourceController.java → 号源/排班
  ├── PatientController.java           → 患者
  └── RegistrationOrderController.java → 订单

DTO （数据传输对象，只传数据不带实体注解）
  ├── ApiResponse.java       → 统一返回 {code, message, data}
  ├── LoginRequest.java      → 登录请求
  ├── RegistrationRequest.java → 挂号请求
  ├── DoctorScheduleVO.java  → 医生排班视图
  └── RegistrationVO.java    → 订单视图（含中文状态名）
```

---

## 三、面试回答速记卡

| 问题 | 一句话回答 |
|------|-----------|
| Spring Boot 优点？ | 自动配置 + Starter 依赖 + 内嵌 Tomcat |
| MyBatis-Plus 解决了什么？ | 自动 CRUD，不用写 XML |
| 事务传播机制你用的哪个？ | `REQUIRED`（默认），保证同事务 |
| GET 和 POST 区别？ | GET 查数据参数在 URL，POST 提交数据参数在 Body |
| 密码怎么存？ | 生产用 BCrypt，我们项目暂用 MD5（待改进） |
| 分了几层？ | Controller → Service → Mapper → DB |
| 超卖怎么解决？ | `UPDATE ... SET count=count-1 WHERE count>0` 原子操作 |
| RESTful 是什么？ | 用 HTTP 方法 + URL 路径操作资源 |

---

## 四、建议学习顺序（按优先级）

1. **Spring Boot 核心** — 自动配置原理、启动流程（B站搜"Spring Boot 自动配置原理"）
2. **MyBatis-Plus** — 条件构造器、分页、自动填充（看官方文档 + 你项目源码）
3. **MySQL + 并发** — 事务隔离级别、乐观锁/悲观锁、超卖问题
4. **JWT + 认证** — 登录流程、Token 签发与校验
5. **RESTful API 设计** — 资源命名、HTTP 方法语义、状态码
6. **Lombok 原理** — 编译期注解处理器
